spring:
  datasource:
    username: test
    password: test
  hibernate:
    dialect: org.hibernate.dialect.PostgreSQLDialect
    ddl-auto: none
  sql:
    init:
      mode: always
  flyway:
    enabled: false